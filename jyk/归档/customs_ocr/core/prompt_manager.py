"""
Prompt 模板管理模块
根据文件类型生成不同的 prompt
"""
from config.field_mapping import get_fields_for_type, ATT_TYPE_NAMES


def generate_prompt(att_type_code: int) -> str:
    """
    根据文件类型代码生成 prompt
    
    Args:
        att_type_code: 文件类型代码
        
    Returns:
        完整的 prompt 字符串
    """
    head_fields, list_fields = get_fields_for_type(att_type_code)
    att_type_name = ATT_TYPE_NAMES.get(att_type_code, "未知文档")
    
    # 构建prompt
    prompt = f"""你是一个专业的报关文档识别助手。请仔细分析这张{att_type_name}图片，按照以下要求提取字段信息：

【重要说明】
1. 输出必须严格遵循JSON格式，不要添加任何markdown标记（如```json）
2. 字段名称（keyDesc）必须从提供的标准名称池中选择，不能自己创造
3. 图片中未识别到的字段不应出现在JSON中
4. 对于表头字段，同一keyDesc只能出现一次
5. 对于表体字段，如果有多个商品，每个商品的字段单独记录在一个数组中
6. pixel必须是归一化坐标，范围[0-999]，格式为[左上x, 左上y, 右下x, 右下y]
7. 图片中的字段名称可能与标准名称不完全一致（如"TO USA"对应"运抵国"），需要你判断对应关系

【表头字段标准名称池】（preDecHead）
"""
    
    if head_fields:
        for i, field in enumerate(head_fields, 1):
            prompt += f"{i}. {field}\n"
    else:
        prompt += "无\n"
    
    prompt += "\n【表体字段标准名称池】（preDecList）\n"
    
    if list_fields:
        for i, field in enumerate(list_fields, 1):
            prompt += f"{i}. {field}\n"
    else:
        prompt += "无\n"
    
    prompt += """
【输出格式】
请输出以下JSON格式（不要添加markdown标记）：
```json
{
  "preDecHead": [
    {
      "keyDesc": "字段标准名称",
      "value": "识别到的值",
      "pixel": [左上x, 左上y, 右下x, 右下y]
    }
  ],
  "preDecList": [
    [
      {
        "keyDesc": "字段标准名称",
        "value": "识别到的值",
        "pixel": [左上x, 左上y, 右下x, 右下y]
      }
    ]
  ]
}
```

请开始识别！
"""
    
    return prompt

def generate_mainfactor_prompt(hsCodes: list, mainfactors: list) -> str:
    prompt = """你是一个专业的报关单证识别助手。你的任务是从单据图片中提取对应商品的申报要素值。

### 核心指令
以下会给出**商品和对应的申报要素**（格式如 "0:品牌类型;1:出口享惠情况;..."）。请严格按照定义中**要素项的下标顺序**，从图片中提取对应的值。按照你的理解，将商品和对应申报要素匹配识别。

### 关键规则（必须遵守）
1. **格式要求**：将提取出的值用 "|" 符号拼接成一个字符串返回，不要包含下标或要素名称。
2. **特殊字段转码**：
   - **品牌类型**（通常是第0项）：
     - 看到“无品牌” -> 输出 `0`
     - 看到“境内自主品牌” -> 输出 `1`
     - 看到“境内收购品牌” -> 输出 `2`
     - 看到“境外品牌（贴牌生产）” -> 输出 `3`
     - 看到“境外品牌（其他）” -> 输出 `4`
   - **出口享惠情况**（通常是第1项）：
     - 看到“不享受优惠关税” -> 输出 `0`
     - 看到“享受优惠关税” -> 输出 `1`
     - 看到“不能确定” -> 输出 `2`
     - 看到“不适用于进口报关单” -> 输出 `3`
3. **空值处理**：如果图片中找不到某项要素（如GTIN、CAS），请在该位置填入空字符串，但分隔符 "|" 必须保留以维持位置正确。
"""
    prompt += f"""### 任务
- 输入的图片中有{len(hsCodes)}条商品信息。
- 提取这{len(hsCodes)}条商品的规格型号信息。
- 每个商品的规格型号有多个属性，需根据商品编码找到对应的规格型号属性。
- 商品编码及其对应的规格型号属性如下：
"""
    for hscode, mainfactor in zip(hsCodes, mainfactors):
        prompt += f""" - “{hscode}”的规格型号属性为：{mainfactor}。\n"""

    prompt += """
### 提取规则
1. 商品信息及其顺序以报关单据中的商品顺序为准，即“商品项号”、“商品编号”、“商品名称”应从报关单据区域的商品信息中提取。
2. 当提供的资料中没有明确标注“商品项号”时，应将“商品项号”的值设定为实际的商品顺序号，从1开始计数。
3. 如果商品规格型号信息不在报关单内，而是存在于另一份资料中，请执行以下操作：
   - 使用“商品编码”和“商品名称”作为关键字。
   - 在规格型号资料中查找与之匹配的记录。
   - 提取对应的规格型号信息。
4. 当某一属性值出现在商品信息的公共区域（如上方或下方）时，该属性值应被视为适用于所有商品，如“品牌类型”、“出口享惠情况”等。
5. 如果“品牌”的属性值后面有“牌”或“无中文名称”等描述时，则将“牌”或“无中文名称”与属性值一起完整地输出，比如出现“SHON牌”，则输出“SHON牌”，比如出现“SHON，无中文名称”，则输出“SHON，无中文名称”。只有当图片中属性值后确实有此关键字时才可以一起输出。
6. 如果“型号”的属性值后面有“等”字时，则将“等”字与属性值一起完整地输出，比如出现“DS-2DE5225W-AE T5等”，则“型号”的属性值应输出“DS-2DE5225W-AE T5等”。只有当图片中属性值后确实有此关键字时才可以一起输出。
### 关键词与属性值对应规则
1. **出口享惠情况**：
   - 关键词：“享惠”、“享受优惠关税”、“不享惠”、“不能确定”
   - 说明：只有当图片中确实存在上述关键词之一时，才将其归类为“出口享惠情况”的属性值。

2. **品牌类型**：
   - 关键词：“境外品牌(贴牌生产)”、“贴牌”、“境外品牌”、“贴牌生产”、“境外品牌(其它)”、“境外品牌(其他)”、“境外其他”、“境外其它”、“境内自主品牌”、“自主品牌”、“境内收购品牌”、“境内收购”、“无品牌类型”
   - 说明：只有当图片中确实存在上述关键词之一时，才将其归类为“品牌类型”的属性值。

3. **用途**：
   - 关键词：“坐”、“坐具”、“座具”、“收纳”、“家具”
   - 说明：只有当图片中确实存在上述关键词之一时，才将其归类为“用途”的属性值。
### 输出要求
1. 必须严格按照任务要求输出，禁止产生其他内容。
2. 必须严格按照图片的原始内容进行结果输出，禁止进行任何原图内容的改动。
3. 所有属性值不准遗漏。
4. 仅输出每个商品编码对应的属性值，禁止输出分析过程。
5. 如果没有识别到上述任何关键词，则不输出任何内容。
6. 严格按照任务中要求的商品数量按实际顺序输出对应的商品规格型号信息，商品编码识别出来若有偏差，自行判断，补充完整。
7. 按照JSON格式输出，不包含任何解释、说明或额外文字。
8. 所有输出必须包含“pixel”字段，格式为[左上x, 左上y, 右下x, 右下y]，表示字段在图片中的位置。
### 输出示例
```json
{
  "gmodel": [
    {
      "codeTs": "商品编码1",
      "mainfactors": "识别到的商品编码对应的规格型号属性值字符串，使用|分隔",
      "pixel": [左上x, 左上y, 右下x, 右下y]
    },
    {
      "codeTs": "商品编码2",
      "mainfactors": "识别到的商品编码对应的规格型号属性值字符串，使用|分隔",
      "pixel": [左上x, 左上y, 右下x, 右下y]
    }
    //如果有多组数据，继续添加更多对象
  ]
}
```

请开始识别！
"""
    return prompt
    # return system_prompt + f"\n 申报要素定义：{mainfactor}请严格按上述定义的顺序提取值。如果是品牌类型或享惠情况，请转为代码数字。若在图片中未识别到相关内容，则不输出任何内容。"